<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group"
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control input-md" %>
  </div>
  <div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "form-control input-md" %>
  </div>
  <div class="form-group">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url, class: "form-control input-md" %>
  </div>

  <div class="form-group">
    <%= f.label :colour %><br>
    <%= f.text_field :colour, class: "form-control input-md" %>
  </div>

  <div class="form-group">
    <%= f.label :price %><br>
    <%= f.text_field :price, class: "form-control input-md" %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Create', class: "btn btn-primary btn-lg" %>
  </div>
 
<% end %>
